<h1>Artist Index</h1>

<% @artists.each do |artist| %>
    <p><%= link_to artist.name, artist %> <%= artist.song_count %> songs:</p>

    <ul>
    <% artist.songs.each do |song| %>
        <li><%= link_to song.title, song %></li>
    <% end %>
    </ul>

<% end %>